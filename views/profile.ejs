<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="/css/main.css">
    <script defer src="/javascript/client.js"></script>

    <title>Robertness - Norge</title>
</head>
<body>

    <%- include('components/sidebar') %>

    <div class="main-content">
        <div class="searchbar">
            <div class="search-container">
                <img src="/gfx/search.svg">
                <input type="text" placeholder="Search for ideas...">
            </div>
            <a class="profile-button" href="profile"></a>
            <img src="/gfx/chevron-down.svg" alt="">
        </div>

        <% if (isEdit) { %>
            <%- include('components/edit-profile') %> <!-- This will show the edit form -->
        <% } else { %>
            <%- include('components/main-profile') %> <!-- This will show the regular profile -->
        <% } %>

    </div>
</body>

</html>