<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/fonts.css">

    <script defer src="/javascript/client.js"></script>

    <title>Robertness - Norge</title>
</head>
<body class="ubuntu-regular">

    <%- include('components/sidebar') %>

    <div class="main-content">
        <%- include('components/searchbar') %>
        <h1>Innleggsdetaljer</h1>

        <div class="post-detail">
            <div class="left"> 
                <div class="image">
                    <img src="/images/uploads/<%= post.image %>" alt="Post Image" class="post-image" />
                </div>
            
            </div>    
            <div class="right">
                <div class="top">
                    <form action="/post/<%= post._id %>/save" method="POST">
                        <% if (hasSaved) { %>
                            <button type="submit">Lagret</button>
                        <% } else { %>
                            <button type="submit">Lagre</button>
                        <% } %>
                        
                    </form>         
                </div>

                <div class="bottom">
                    <h2 class="roboto-regular">Tittel</h2>
                    <p> <strong><%= postText %></strong></p>
                    <% if (Description) { %>
                        <br>
                        <p> <%= Description %></p>      
                    <% } %>
                    <div class="postUser">
                        
                        <a class="profilepicpost">
                            <img src="<%= profilepicture %>" alt="">
                        </a>            
                        <p> <%= author %></p>    
                    </div>
                    <h2 class="roboto-regular">Date created</h2>
                    <p class="createdAt"><%= createdAt.toLocaleString() %></p>      
                </div>

            </div>
        </div>

    </div>
</body>

</html>